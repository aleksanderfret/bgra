# --- Node / TypeScript ---
node_modules/
.pnpm-store/
.next/
out/
build/
dist/
coverage/
*.tsbuildinfo
next-env.d.ts
.turbo/

# --- Python ---
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Environment ---
.env
.env.*
!.env.example

# --- Local knowledge base and generated artifacts ---
# Rulebooks are copyrighted material: they stay on your disk, never in git.
# Ignore the contents, keep the directory: git will not descend into an
# ignored directory, so the pattern has to target its entries.
services/rag-engine/storage/*
!services/rag-engine/storage/.gitkeep

# --- Model weights (tens of GB, always fetched locally) ---
models/
*.gguf
*.bin
*.onnx

# --- Editor / OS ---
.DS_Store
*.pem
*.log
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
