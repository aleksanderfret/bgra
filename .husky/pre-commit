# Lint and format only the files in this commit. Fixes are re-staged.
# Types, tests and the production build run on pre-push (`pnpm verify`).
. "$(dirname "$0")/load-node.sh"
corepack pnpm exec lint-staged
