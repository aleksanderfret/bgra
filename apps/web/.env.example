# Copy to .env.local to override.

# Where the Python engine listens. Only the Next.js server reads this; the
# browser never learns the address because every call goes through
# /api/engine/* on this same origin.
RAG_ENGINE_URL=http://127.0.0.1:8000
